# 画面設計書

## 1. 文書の目的

この文書は、ToDo管理アプリの画面設計書（画面の見た目・項目・動きの決めごとをまとめた文書）です。  
`handout_app_spec.md`・`requirements.md`・`db-design.md` をもとに、ToDo機能の**6つの画面**の表示内容、画面遷移（画面から次の画面へ移る流れ）、入力項目、表示文言を整理します。

## 2. 画面一覧

トップページ（`/`）はアプリ全体の入口として存在しますが、ToDo機能の6画面には含めません。本書で設計するのは、次のToDo機能の6画面です。

| No. | 画面名 | URL | 役割 |
|---|---|---|---|
| 1 | 一覧画面 | `/todos` | 登録済みのやることを一覧表示し、検索・並び替え・編集・削除の入口にする |
| 2 | 登録画面 | `/todos/new` | 新しいやることを入力する |
| 3 | 登録確認画面 | `/todos/confirm` | 登録前に入力内容を確認する |
| 4 | 編集画面 | `/todos/{id}/edit` | 既存のやることを修正する |
| 5 | 編集確認画面 | `/todos/{id}/confirm` | 保存前に修正内容を確認する |
| 6 | 削除確認画面 | `/todos/{id}/delete` | 削除前に対象データを確認する |

`{id}` は、やること1件ごとの番号です。

## 3. 画面共通ルール

### 3.1 画面タイトル

各画面の見出し（ページの大きな題名）は、次の文言にします。

| 画面名 | 画面タイトル |
|---|---|
| 一覧画面 | ToDo一覧 |
| 登録画面 | ToDo登録 |
| 登録確認画面 | ToDo登録確認 |
| 編集画面 | ToDo編集 |
| 編集確認画面 | ToDo編集確認 |
| 削除確認画面 | ToDo削除確認 |

### 3.2 画面に出す共通文言

- 優先度は `1 / 2 / 3` をそのまま出さず、`高 / 中 / 低` と表示する
- 完了状態は、DB（データベース）の保存値である `FALSE / TRUE` や MySQL内部の `0 / 1` をそのまま出さず、`未完了 / 完了` と表示する
- 未入力の値を確認画面で見せるときは `（入力なし）` と表示する

### 3.3 共通の入力チェック

入力チェック（入力内容が正しいか確かめること）は、登録画面と編集画面で共通です。

| 項目 | 条件 | エラーメッセージ |
|---|---|---|
| やること | 未入力 | やることを入力してください |
| やること | 255文字超過 | やることは255文字以内で入力してください |
| メモ | 255文字超過 | メモは255文字以内で入力してください |
| ジャンル | 未選択 | ジャンルを選んでください |
| 優先度 | 未選択 | 優先度を選んでください |

- 空白だけの入力は未入力として扱います。
- 期限には入力チェックを設けません。
- エラーメッセージは、対象の入力欄のすぐ下に表示します。

### 3.4 共通の選択肢

ジャンル（分類）は次の5つ固定です。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

優先度は次の3つ固定です。

- 高
- 中
- 低

## 4. 画面遷移一覧

画面遷移（画面から次の画面へ移る流れ）は次のとおりです。

| 画面 | 操作 | 遷移先 | 備考 |
|---|---|---|---|
| 一覧画面 | `新規登録` | 登録画面 | 新規入力へ進む |
| 一覧画面 | `編集` | 編集画面 | 対象行の `id` を使う |
| 一覧画面 | `削除` | 削除確認画面 | 対象行の `id` を使う |
| 一覧画面 | `絞り込む` | 一覧画面 | 検索条件を反映して再表示する |
| 一覧画面 | `期限が近い順` | 一覧画面 | `due_date` 昇順（早い順）で再表示する |
| 一覧画面 | `期限が遠い順` | 一覧画面 | `due_date` 降順（遅い順）で再表示する |
| 登録画面 | `確認する` | 登録確認画面 | 入力が正しいときだけ進む |
| 登録画面 | `一覧へ` | 一覧画面 | 入力途中でも戻れる |
| 登録確認画面 | `登録する` | 一覧画面 | 登録後に `登録しました` を表示する |
| 登録確認画面 | `書き直す` | 登録画面 | 入力済みの値を保持したまま戻る |
| 編集画面 | `確認する` | 編集確認画面 | 入力が正しいときだけ進む |
| 編集画面 | `一覧へ` | 一覧画面 | 保存せずに戻る |
| 編集確認画面 | `保存する` | 一覧画面 | 保存後に `保存しました` を表示する |
| 編集確認画面 | `書き直す` | 編集画面 | 入力済みの値を保持したまま戻る |
| 削除確認画面 | `消す` | 一覧画面 | 削除後に `削除しました` を表示する |
| 削除確認画面 | `やめる` | 一覧画面 | 削除せずに戻る |

存在しない `id` が指定された場合は、専用画面を出さず、一覧画面へ戻して `見つかりませんでした` を表示します。

## 5. 画面詳細

### 5.1 一覧画面 `/todos`

#### 画面の目的

登録済みのToDoをまとめて確認し、検索・並び替え・登録・編集・削除へ進むための中心画面です。

#### 表示項目

1. 画面タイトル: `ToDo一覧`
2. 操作結果メッセージ表示欄
3. 検索エリア
4. 並び替えリンク
5. `新規登録` ボタン
6. ToDo一覧表

#### 操作結果メッセージ

画面上部に次のいずれかを1行表示します。

- 登録しました
- 保存しました
- 削除しました
- 見つかりませんでした

表示対象がないときは、この欄は表示しません。

#### 検索エリアの項目

| 項目 | 画面に出す言葉 | 入力形式 |
|---|---|---|
| キーワード検索 | やること | 1行の文字入力 |
| ジャンル検索 | ジャンル | プルダウン（選ぶ欄） |
| 検索実行 | 絞り込む | ボタン |

ジャンル検索の選択肢は次のとおりです。

- すべて
- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

#### 並び替えリンク

- `期限が近い順`
- `期限が遠い順`

初回表示は `期限が近い順` です。

#### 一覧表

表の列見出し（表の上に出る項目名）は次のとおりです。

| 列順 | 列見出し | 内容 |
|---|---|---|
| 1 | やること | `title` |
| 2 | ジャンル | `category` |
| 3 | 優先度 | `priority` を `高 / 中 / 低` に変換して表示 |
| 4 | 期限 | `due_date` |
| 5 | 状態 | `completed` を `完了 / 未完了` に変換して表示 |
| 6 | 操作 | `編集` リンク、`削除` リンク |

#### 一覧が0件のとき

一覧表の代わりに、次の文言を表示します。

- まだ1件も登録されていません

#### 画面に出すボタン・リンク名

- `絞り込む`
- `期限が近い順`
- `期限が遠い順`
- `新規登録`
- `編集`
- `削除`

#### この画面からの遷移

- `新規登録` で登録画面へ進む
- 行ごとの `編集` で編集画面へ進む
- 行ごとの `削除` で削除確認画面へ進む
- `絞り込む` で同じ一覧画面を条件付きで再表示する
- `期限が近い順` / `期限が遠い順` で同じ一覧画面を並び順付きで再表示する

### 5.2 登録画面 `/todos/new`

#### 画面の目的

新しいToDoを入力する画面です。

#### 表示項目

1. 画面タイトル: `ToDo登録`
2. 入力フォーム
3. 操作ボタン

#### 入力項目

| 項目 | ラベル | 必須 | 入力形式 | 初期値 |
|---|---|---|---|---|
| やること | やること | 必須 | 1行の文字入力 | 空 |
| メモ | メモ | 任意 | 1行の文字入力 | 空 |
| ジャンル | ジャンル | 必須 | プルダウン | 空 |
| 優先度 | 優先度 | 必須 | プルダウン | 空 |
| 期限 | 期限 | 任意 | 日付入力 | 空 |

ジャンルの選択肢は次の5つです。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

優先度の選択肢は次の3つです。

- 高
- 中
- 低

#### エラー表示

入力エラー時は、入力欄の下に次の文言を表示します。

- やることを入力してください
- やることは255文字以内で入力してください
- メモは255文字以内で入力してください
- ジャンルを選んでください
- 優先度を選んでください

#### 画面に出すボタン名

- `確認する`
- `一覧へ`

#### この画面からの遷移

- `確認する` で登録確認画面へ進む
- 入力エラーがある場合は、この画面に留まる
- `一覧へ` で一覧画面へ戻る

### 5.3 登録確認画面 `/todos/confirm`

#### 画面の目的

登録前に、入力した内容を見直す画面です。

#### 表示項目

1. 画面タイトル: `ToDo登録確認`
2. 入力内容の確認表示
3. 操作ボタン

#### 確認表示する項目

| 表示名 | 表示内容 |
|---|---|
| やること | 入力した `title` |
| メモ | 入力した `detail`。未入力なら `（入力なし）` |
| ジャンル | 入力した `category` |
| 優先度 | 入力した優先度を `高 / 中 / 低` で表示 |
| 期限 | 入力した `due_date`。未入力なら `（入力なし）` |

#### 画面に出すボタン名

- `登録する`
- `書き直す`

#### この画面からの遷移

- `登録する` で登録処理を行い、一覧画面へ戻る
- 一覧画面では `登録しました` を表示する
- `書き直す` で登録画面へ戻る
- 戻るときは入力済みの値を保持する

### 5.4 編集画面 `/todos/{id}/edit`

#### 画面の目的

登録済みのToDoを修正する画面です。

#### 表示項目

1. 画面タイトル: `ToDo編集`
2. 入力フォーム
3. 操作ボタン

#### 入力項目

| 項目 | ラベル | 必須 | 入力形式 | 初期表示 |
|---|---|---|---|---|
| やること | やること | 必須 | 1行の文字入力 | 現在値を表示 |
| メモ | メモ | 任意 | 1行の文字入力 | 現在値を表示 |
| ジャンル | ジャンル | 必須 | プルダウン | 現在値を選択した状態で表示 |
| 優先度 | 優先度 | 必須 | プルダウン | 現在値を選択した状態で表示 |
| 期限 | 期限 | 任意 | 日付入力 | 現在値を表示 |
| 完了 | 完了 | 任意 | チェックボックス（チェックの有無で選ぶ欄） | 現在値を表示 |

#### エラー表示

入力エラー時は、登録画面と同じ文言を入力欄の下に表示します。

- やることを入力してください
- やることは255文字以内で入力してください
- メモは255文字以内で入力してください
- ジャンルを選んでください
- 優先度を選んでください

#### 画面に出すボタン名

- `確認する`
- `一覧へ`

#### この画面からの遷移

- `確認する` で編集確認画面へ進む
- 入力エラーがある場合は、この画面に留まる
- `一覧へ` で一覧画面へ戻る
- 存在しない `id` の場合は一覧画面へ戻り、`見つかりませんでした` を表示する

### 5.5 編集確認画面 `/todos/{id}/confirm`

#### 画面の目的

保存前に、修正内容を見直す画面です。

#### 表示項目

1. 画面タイトル: `ToDo編集確認`
2. 修正内容の確認表示
3. 操作ボタン

#### 確認表示する項目

| 表示名 | 表示内容 |
|---|---|
| やること | 入力した `title` |
| メモ | 入力した `detail`。未入力なら `（入力なし）` |
| ジャンル | 入力した `category` |
| 優先度 | 入力した優先度を `高 / 中 / 低` で表示 |
| 期限 | 入力した `due_date`。未入力なら `（入力なし）` |
| 完了 | `完了` または `未完了` |

#### 画面に出すボタン名

- `保存する`
- `書き直す`

#### この画面からの遷移

- `保存する` で更新処理を行い、一覧画面へ戻る
- 一覧画面では `保存しました` を表示する
- `書き直す` で編集画面へ戻る
- 戻るときは入力済みの値を保持する

### 5.6 削除確認画面 `/todos/{id}/delete`

#### 画面の目的

削除対象が正しいかを確認して、削除するか中止するかを選ぶ画面です。

#### 表示項目

1. 画面タイトル: `ToDo削除確認`
2. 削除対象の表示
3. 操作ボタン

#### 表示する項目

| 表示名 | 表示内容 |
|---|---|
| やること | `title` |
| ジャンル | `category` |
| 期限 | `due_date`。未入力なら `（入力なし）` |

入力欄は表示しません。

#### 画面に出すボタン名

- `消す`
- `やめる`

#### この画面からの遷移

- `消す` で削除処理を行い、一覧画面へ戻る
- 一覧画面では `削除しました` を表示する
- `やめる` で一覧画面へ戻る
- 存在しない `id` の場合は一覧画面へ戻り、`見つかりませんでした` を表示する

## 6. URLパラメータ設計

URLパラメータ（URLの末尾に付ける条件）は、一覧画面だけで使います。

| パラメータ名 | 意味 | 例 |
|---|---|---|
| `keyword` | やること名での絞り込み | `keyword=課題` |
| `category` | ジャンルでの絞り込み | `category=プログラミング` |
| `order` | 並び順 | `order=asc` / `order=desc` |

利用例:

- `/todos?keyword=課題`
- `/todos?category=資格`
- `/todos?keyword=課題&category=プログラミング&order=asc`

`order=asc` は期限が近い順、`order=desc` は期限が遠い順です。

## 7. 画面設計上の補足

- トップページ `/` はアプリ全体の入口として存在しますが、ToDo機能の6画面には含めず、本書の設計対象外です
- 画面ごとの見た目の詳細、CSS（見た目を整える設定）、class名（見た目を指定するための名前）は、`requirements.md` と `handout_app_spec.md` の指定を正とします
- ToDo機能の6画面で使う主要なclass名は、一覧画面が `container`、`page-title`、`todo-table`、`flash`、`search-bar`、登録・編集画面が `container`、`page-title`、`form-row`、`form-label`、`form-input`、`error`、各確認画面が `container`、`page-title`、`confirm-value`、`confirm-actions`、ボタンが `btn` または `btn sub` です
- `id`、`created_at`、`updated_at` は入力欄として表示しません
- ログイン画面は作りません
- 検索結果が0件でも、専用メッセージは増やさず `まだ1件も登録されていません` を表示します
