# 要件定義書

## 1. 文書の目的 [追加]

この文書は、ToDo管理アプリで「何を作るか」を決めるための要件定義書です。  
要件定義書（作るものの約束ごとをまとめた文書）として、機能、画面、保存する情報、入力の決まりを整理します。

## 2. 対象システムの概要 [追加]

- 対象システムは、やることを登録して管理するToDo管理アプリです。
- 利用者は自分1人です。
- ログイン機能は作りません。

## 3. 本書の前提と範囲 [追加]

- 本書では要件定義のみを扱います。
- DB設計書（データベースの詳しい設計書）と画面設計書（画面の詳しい設計書）は別途作成します。
- 本書に書かれていない機能は、今回は対象外です。

## 4. 機能要件

### 4.1 提供する機能

本アプリで提供する機能は、以下の6つです。

| No. | 機能名 | 内容 |
|---|---|---|
| 1 | 一覧表示 | 登録したやることを1画面に一覧表示する |
| 2 | 登録 | 新しいやることを1件登録する |
| 3 | 編集 | 登録済みのやることを修正する |
| 4 | 削除 | 不要になったやることを削除する |
| 5 | 検索 | やることの名前で一覧を絞り込む |
| 6 | 並び替え | 期限の順で一覧を並び替える |

### 4.2 画面一覧

トップページ（`/`）はアプリ全体の入口です。ToDo機能の画面は、トップページを含めない6画面です。

| 画面名 | URL |
|---|---|
| トップページ | `/` |
| 一覧画面 | `/todos` |
| 登録画面 | `/todos/new` |
| 登録確認画面 | `/todos/confirm` |
| 編集画面 | `/todos/{id}/edit` |
| 編集確認画面 | `/todos/{id}/confirm` |
| 削除確認画面 | `/todos/{id}/delete` |

`{id}` は、各やることを見分ける番号です。

### 4.3 画面遷移要件

画面遷移（画面どうしの移動）は以下のとおりです。

- 一覧画面から登録画面へ移動できること
- 登録画面から登録確認画面へ移動できること
- 登録確認画面から登録処理を実行後、一覧画面へ戻ること
- 登録確認画面から書き直し操作で、入力値を保持したまま登録画面へ戻れること
- 一覧画面から編集画面へ移動できること
- 編集画面から編集確認画面へ移動できること
- 編集確認画面から保存処理を実行後、一覧画面へ戻ること
- 編集確認画面から書き直し操作で、入力値を保持したまま編集画面へ戻れること
- 一覧画面から削除確認画面へ移動できること
- 削除確認画面から削除処理を実行後、一覧画面へ戻ること
- 削除確認画面からやめる操作で、一覧画面へ戻れること
- 登録画面と編集画面から「一覧へ」で一覧画面へ戻れること

## 5. データ要件

### 5.1 保存先

- 保存先はMySQL（データを保存する仕組み）の `todos` テーブルです。
- 1件のやることが1行に対応します。

### 5.2 保存する項目

| 項目名 | 内容 | 型 | 必須 | 備考 |
|---|---|---|---|---|
| `id` | 1件を見分ける番号 | `BIGINT` | 必須 | 自動採番（自動で番号を振ること）、主キー（行を一意に決める列） |
| `title` | やること | `VARCHAR(255)` | 必須 | 255文字以内 |
| `detail` | メモ | `VARCHAR(255)` | 任意 | 255文字以内 |
| `category` | ジャンル | `VARCHAR(255)` | 必須 | 指定の5種類から選択 |
| `priority` | 優先度 | `INT` | 必須 | 1=高、2=中、3=低。既定値（初期値）は2 |
| `due_date` | 期限 | `DATE` | 任意 | 入力制約なし |
| `completed` | 完了状態 | `BOOLEAN` | 必須 | 既定値は `FALSE`（未完了）。MySQLでは内部的に `0` / `1` として扱われることがある |
| `created_at` | 登録日時 | `DATETIME` | 必須 | MySQLが自動設定 |
| `updated_at` | 更新日時 | `DATETIME` | 必須 | MySQLが自動設定 |

### 5.3 選択肢が固定の項目

`category` は自由入力ではなく、以下の5つから選択します。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

### 5.4 利用者が入力しない項目

以下の項目は入力欄に表示せず、保存時に自動で設定します。

- `id`
- `created_at`
- `updated_at`

## 6. 画面要件

### 6.1 一覧画面 `/todos`

- やること、ジャンル、優先度、期限、状態、操作を一覧表示すること
- 画面上部に、名前検索欄、ジャンル絞り込み欄、「絞り込む」ボタン、並び替えリンク2本、「新規登録」ボタンを表示すること
- 優先度は保存値ではなく、画面上では「高 / 中 / 低」と表示すること
- 完了状態は保存値ではなく、画面上では「完了 / 未完了」と表示すること
- 登録データが0件のときは「まだ1件も登録されていません」と表示すること
- 画面最上部に、直前の操作結果を1行表示すること

### 6.2 登録画面 `/todos/new`

- 入力項目は「やること」「メモ」「ジャンル」「優先度」「期限」とすること
- 「やること」「ジャンル」「優先度」は必須入力とすること
- 「メモ」「期限」は任意入力とすること
- ジャンルは5つから選ぶ方式とし、初期表示は空欄とすること
- 優先度は「高・中・低」から選ぶ方式とし、初期表示は空欄とすること
- 操作は「確認する」「一覧へ」を用意すること

### 6.3 登録確認画面 `/todos/confirm`

- 入力された5項目を文字として表示すること
- 未入力の項目は「（入力なし）」と表示すること
- 操作は「登録する」「書き直す」を用意すること
- 「書き直す」では入力値を保持したまま登録画面へ戻すこと

### 6.4 編集画面 `/todos/{id}/edit`

- 登録画面の5項目に加え、「完了」の入力項目を表示すること
- 「完了」はチェックを入れる方式とすること
- 初期表示時に、現在の登録内容を各項目へ表示すること
- 必須入力は「やること」「ジャンル」「優先度」とすること
- 操作は「確認する」「一覧へ」を用意すること

### 6.5 編集確認画面 `/todos/{id}/confirm`

- 表示形式は登録確認画面と同じとすること
- 操作は「保存する」「書き直す」を用意すること

### 6.6 削除確認画面 `/todos/{id}/delete`

- 削除対象の「やること」「ジャンル」「期限」を表示すること
- 入力欄は表示しないこと
- 操作は「消す」「やめる」を用意すること

## 7. 入力要件

### 7.1 入力チェック

入力チェック（入力内容の確認）は以下のとおりです。

| 項目 | 条件 | 表示メッセージ |
|---|---|---|
| やること | 未入力 | やることを入力してください |
| やること | 255文字超過 | やることは255文字以内で入力してください |
| メモ | 255文字超過 | メモは255文字以内で入力してください |
| ジャンル | 未選択 | ジャンルを選んでください |
| 優先度 | 未選択 | 優先度を選んでください |

- 期限には入力チェックを設けません。
- 必須項目が空の場合は保存せず、元の入力画面に留まること
- エラーメッセージ（入力エラーを知らせる文）は対象欄の下に表示すること

### 7.2 空白の扱い

- 半角空白だけ、または全角空白だけの入力は未入力として扱うこと

## 8. 検索・並び替え要件

### 8.1 検索条件

- 名前検索は `title` に入力語が一部でも含まれるデータを対象とすること
- ジャンル検索は `category` が完全一致するデータを対象とすること
- 名前検索とジャンル検索の両方が空の場合は、全件表示すること

### 8.2 並び替え条件

- 並び替えは `due_date` を基準に行うこと
- 初回表示時は、期限が近い順で表示すること
- 並び替えは「期限が近い順」「期限が遠い順」の2種類とすること

### 8.3 検索結果0件時の表示

- 絞り込み結果が0件の場合も、「まだ1件も登録されていません」と表示すること

### 8.4 URLによる条件保持

- 検索条件と並び順は、クエリパラメータ（URLの `?` の後ろに付く条件）で保持すること
- 例：`/todos?keyword=課題&category=プログラミング&order=asc`

## 9. 例外時の要件

### 9.1 存在しないID指定時

- 編集画面または削除確認画面で、存在しない `id` を指定された場合は一覧画面へ戻すこと
- その際、画面上部に「見つかりませんでした」と表示すること
- 専用のエラー画面は作らないこと

## 10. 完了後メッセージ要件

一覧画面の最上部に、直前の操作結果として以下の文言を表示します。

| タイミング | 表示文言 |
|---|---|
| 登録後 | 登録しました |
| 編集保存後 | 保存しました |
| 削除後 | 削除しました |
| 存在しないID指定時 | 見つかりませんでした |

## 11. ログ要件

- 登録、編集、削除が実行されたとき、各1行のログ（動作記録）を残すこと
- ログに残す内容は、操作種別（登録 / 編集 / 削除）と対象データの `id` とすること
- `title` と `detail` の中身はログに残さないこと

## 12. 画面共通デザイン要件

### 12.1 CSSの適用方針

- CSS（見た目を整える設定）は `src/main/resources/static/css/style.css` の1ファイルだけを使用すること
- ToDo機能の6画面すべてで同じCSSを読み込むこと。トップページはこの6画面に含めないこと
- 各テンプレートの `<head>` に `<link rel="stylesheet" href="/css/style.css">` を記載すること
- 画面内へ直接 `style="..."` を書かないこと
- CSSファイルを追加しないこと

### 12.2 使用するclass名

以下のclass名だけを使用し、新しいclass名は追加しません。

| 用途 | class名 | 対象画面 |
|---|---|---|
| 画面全体の入れ物 | `container` | ToDo機能の6画面 |
| 画面タイトル | `page-title` | ToDo機能の6画面 |
| 一覧表 | `todo-table` | 一覧画面 |
| 操作結果メッセージ | `flash` | 一覧画面 |
| 検索・並び替えエリア | `search-bar` | 一覧画面 |
| 入力欄1行 | `form-row` | 登録画面、編集画面 |
| ラベル | `form-label` | 登録画面、編集画面 |
| 入力欄・選択欄 | `form-input` | 登録画面、編集画面 |
| エラーメッセージ | `error` | 登録画面、編集画面 |
| 確認画面の値表示 | `confirm-value` | 登録確認画面、編集確認画面、削除確認画面 |
| 確認画面のボタン並び | `confirm-actions` | 登録確認画面、編集確認画面、削除確認画面 |
| 主ボタン | `btn` | ToDo機能の6画面 |
| 副ボタン | `btn sub` | ToDo機能の6画面 |
| 文字リンク | `link` | 一覧画面 |

### 12.3 見た目に関する制約

- CSSの内容は叩き台の指定どおりとすること
- 影、動き、画像、外部CSSは使用しないこと

## 13. 非機能要件 [追加]

### 13.1 利用環境

- ローカル環境（自分のパソコン上の動作環境）で、`http://localhost:8080` から利用できること

### 13.2 運用方針

- 個人利用を前提とし、複数人での同時利用は対象外とすること

## 14. 今回の対象外

- 一覧のページ分け
- 削除データの復元
- 完了日時の記録
- ログイン機能
- 複数人での共有
- 写真やファイルの添付

## 15. 叩き台からの補足一覧 [追加]

この要件定義書では、叩き台にない補足として以下を追加しました。

- 「1. 文書の目的」: この文書の役割を明記
- 「2. 対象システムの概要」: アプリ全体の短い説明を追加
- 「3. 本書の前提と範囲」: 今回どこまでを対象にするかを明記
- 「13. 非機能要件」: 利用環境と運用前提を整理
